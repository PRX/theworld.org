/**
 * @file ForwardButton.style.ts
 * Styles and theme for ForwardButton.
 */

import { createStyles, makeStyles } from '@mui/styles';

export const useForwardButtonStyles = makeStyles(() =>
  createStyles({
    root: {},
    iconRoot: {
      fontSize: 'inherit'
    }
  })
);
