/**
 * @file ReplayButton.style.ts
 * Styles and theme for ReplayButton.
 */

import { createStyles, makeStyles } from '@mui/styles';

export const useReplayButtonStyles = makeStyles(() =>
  createStyles({
    root: {},
    iconRoot: {
      fontSize: 'inherit'
    }
  })
);
