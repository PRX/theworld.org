/**
 * @file TimeInfo.style.ts
 * Styles and theme for TimeInfo.
 */

import { makeStyles } from 'tss-react/mui';

export const useTimeInfoStyles = makeStyles()(() => ({
  root: {
    whiteSpace: 'nowrap'
  }
}));
