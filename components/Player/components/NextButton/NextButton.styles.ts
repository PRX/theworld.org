/**
 * @file NextButton.style.ts
 * Styles and theme for NextButton.
 */

import { makeStyles } from 'tss-react/mui';

export const useNextButtonStyles = makeStyles()(() => ({
  root: {},

  iconRoot: {
    fontSize: 'inherit'
  }
}));
