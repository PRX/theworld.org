/**
 * @file PreviousButton.style.ts
 * Styles and theme for PreviousButton.
 */

import { makeStyles } from 'tss-react/mui';

export const usePreviousButtonStyles = makeStyles()(() => ({
  root: {},

  iconRoot: {
    fontSize: 'inherit'
  }
}));
