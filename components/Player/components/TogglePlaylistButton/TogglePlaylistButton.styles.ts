/**
 * @file TogglePlaylistButton.style.ts
 * Styles and theme for TogglePlaylistButton.
 */

import { createStyles, makeStyles } from '@mui/styles';

export const useTogglePlaylistButtonStyles = makeStyles(() =>
  createStyles({
    root: {},
    iconRoot: {
      fontSize: 'inherit'
    }
  })
);
