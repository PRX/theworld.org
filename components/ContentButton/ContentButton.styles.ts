/**
 * @file ContentButton.style.ts
 * Styles for ContentButton.
 */

import { createStyles, makeStyles } from '@material-ui/core/styles';

export const contentButtonStyles = makeStyles(() =>
  createStyles({
    root: {}
  })
);
